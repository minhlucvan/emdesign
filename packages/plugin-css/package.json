{
  "name": "@emdesign/plugin-css",
  "version": "0.1.0-alpha.0",
  "type": "module",
  "description": "emdesign CSS plugin — owns CSS->graph parsing (tokens, themes, colors) + new node types (breakpoint, mediaQuery, cssVarGroup, contrastPair) + CSS production-readiness doctor rules.",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@emdesign/plugin-api": "^0.1.0-alpha.0",
    "@emdesign/graph": "^0.1.0-alpha.0",
    "@emdesign/dsr": "^0.1.0-alpha.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
