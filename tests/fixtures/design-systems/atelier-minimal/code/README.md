# Atelier Minimal — Code Primitives

Test fixture for emdesign. See `DESIGN.md` for token contract.

## Available Primitives

This minimal fixture does not include full primitives. Use the real
`design-systems/atelier/` for full primitive coverage in integration tests.
